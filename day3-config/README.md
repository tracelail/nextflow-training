# Day 3 - Configuration & Process Directives

## What is nextflow.config?
Configuration separates what to do (workflow logic) from how to do it (execution settings). The nextflow.config file controls resource allocation, execution environment, and parameter defaults.

